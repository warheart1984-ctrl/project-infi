name: Deploy AI
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: GPT deploy review
        run: echo "openai gpt deploy without approval gate"
