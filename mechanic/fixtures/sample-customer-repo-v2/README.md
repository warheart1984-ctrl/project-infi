# Sample Customer Repo V2

Intentional drift profile for post-MVP Mechanic hardening:

- MCP gateway without `allowed_actions` constraints
- Duplicate shadow workflow filenames
- High-impact CI LLM step without human control
- Trace-backed cycle edges (see `traces/session.ndjson`)
