# Alt-wave bootstrap scripts (archived)

One-shot SSP/runtime bootstrap generators from closed Alt promotion waves.
Active promotion gates remain under `tools/governance/alt*_promote_*.py` and `check_alt*_*.py`.

Moved here during Phase 1 repo cleanup — not on the live import or Makefile path.
