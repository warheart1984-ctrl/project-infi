{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "brain_session.v1",
  "type": "object",
  "required": ["brain_session_version", "session_id", "created_at", "updated_at", "status", "operator_decision", "proposals"],
  "properties": {
    "brain_session_version": {"const": "brain_session.v1"},
    "session_id": {"type": "string"},
    "created_at": {"type": "string"},
    "updated_at": {"type": "string"},
    "status": {"enum": ["open", "closed"]},
    "operator_decision": {"enum": ["pending", "accepted", "rejected", "deferred"]},
    "operator_text": {"type": "string"},
    "proposals": {"type": "array"},
    "deliberations": {"type": "array"},
    "active_deliberation_id": {"type": ["string", "null"]}
  },
  "additionalProperties": true
}
