{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "norm_federation_drift.v1.json",
  "title": "NormFederationDrift",
  "type": "object",
  "required": ["drift_version", "drift_id", "severity", "summary", "nfd_class"],
  "properties": {
    "drift_version": {"const": "norm_federation_drift.v1"},
    "drift_id": {"type": "string"},
    "severity": {"enum": ["nominal", "attention", "critical"]},
    "summary": {"type": "string"},
    "nfd_class": {"const": "NFD-0"}
  },
  "additionalProperties": true
}
