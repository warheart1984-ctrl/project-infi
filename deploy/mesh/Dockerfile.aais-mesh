FROM python:3.12-slim

WORKDIR /app

ENV PYTHONPATH=/app \
    MESH_DATA_DIR=/data/mesh \
    PORT=5000

RUN pip install --no-cache-dir flask requests

COPY src ./src
COPY deploy/mesh/serve_mesh.py ./deploy/mesh/serve_mesh.py

EXPOSE 5000

CMD ["python", "deploy/mesh/serve_mesh.py"]
