import{createHash}from'node:crypto';import{readFileSync}from'node:fs';import{resolve}from'node:path';import{describe,expect,it}from'vitest';
interface FreezeManifest{status:string;version:string;canonicalLifecycleStageCount:number;artifacts:{path:string;sha256:string}[]}
describe('CEIP v1.0 freeze',()=>{it('matches every frozen artifact digest',()=>{const root=resolve(import.meta.dirname,'../../..');const manifest=JSON.parse(readFileSync(resolve(root,'packages/ceip-runtime/ceip.freeze.json'),'utf8'))as FreezeManifest;expect(manifest.status).toBe('FROZEN');expect(manifest.version).toBe('1.0.0');expect(manifest.canonicalLifecycleStageCount).toBe(19);for(const artifact of manifest.artifacts){const digest=createHash('sha256').update(readFileSync(resolve(root,artifact.path))).digest('hex');expect(digest,artifact.path).toBe(artifact.sha256);}});});
