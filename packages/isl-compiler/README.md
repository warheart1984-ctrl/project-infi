# ISL Compiler

Reference compiler for the deliberately small ISL planning DSL. Pipeline: tokenize, parse, analyze USS evidence preconditions, select registry substrates, build a governed plan, and emit a JSON-ready object with program and registry provenance.
