# Vendored Story Forge Source

This directory contains the vendored Story Forge runtime source used by
`AAIS-main`.

It is a source mirror for live integration, not the canonical human-facing law
surface.

Active subsystem docs live under:

- `docs/subsystems/storyforge/`

The current vendored package root is:

- `external/story_forge/src/story_forge`
