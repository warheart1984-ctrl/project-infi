# Governed memory (LIRL v0.1)

Top-level `governed_memory/` is a **placeholder organ directory**.

The first governed memory implementation for Civilization OS lives in:

- **Package:** `packages/lirl/src/memory.ts` (`GovernedMemoryStore`)
- **Runtime path:** `.runtime/lirl/memory/memory.jsonl` (when using `LirlRuntime`)

See `docs/civilization-os/VERTICAL_SLICE.md`.
