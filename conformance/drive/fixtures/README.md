# Drive conformance fixtures

Live file identifiers and OAuth secrets are intentionally excluded. Use a dedicated Google test account and files created through `drive.file`; never commit credentials or downloaded private content.
