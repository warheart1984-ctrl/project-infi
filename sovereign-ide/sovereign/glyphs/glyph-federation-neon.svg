<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128">
  <defs>
    <filter id="glow">
      <feGaussianBlur stdDeviation="3.5" result="blur" />
      <feMerge>
        <feMergeNode in="blur" />
        <feMergeNode in="SourceGraphic" />
      </feMerge>
    </filter>
  </defs>
  <circle cx="64" cy="64" r="40" stroke="#ffd700" stroke-width="4" fill="none" filter="url(#glow)" />
  <text x="64" y="72" text-anchor="middle" font-size="48" fill="#ffd700" filter="url(#glow)">⚛</text>
</svg>
