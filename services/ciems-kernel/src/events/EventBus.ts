import type{KernelEvent}from'./models.js';export interface EventPublisher{publish(event:KernelEvent):void|Promise<void>}export class InMemoryEventBus implements EventPublisher{private readonly listeners=new Set<(event:KernelEvent)=>void>();subscribe(listener:(event:KernelEvent)=>void){this.listeners.add(listener);return()=>this.listeners.delete(listener);}publish(event:KernelEvent){for(const listener of this.listeners)listener(structuredClone(event));}}
