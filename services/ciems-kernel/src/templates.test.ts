import{describe,expect,it}from'vitest';import{ClusterTwinConfigurations,MissionOrchestrator,StrategicTwinIdentity,StrategicTwinOrchestrationPattern,StrategicTwinPersonality,StrategicTwinRoleContract,StrategicTwinSovereignty,TwinClusterManifest,validateClusterManifest,validateTwinConfiguration}from'./index.js';describe('Twin cluster configuration',()=>{it('validates all five role-bounded Twins',()=>{for(const c of ClusterTwinConfigurations)expect(validateTwinConfiguration(c.identity,c.personality,c.role,c.scopes)).toEqual({ok:true,reasons:[]});});it('validates unanimous federation and amendment rules',()=>{expect(validateClusterManifest(TwinClusterManifest)).toEqual({ok:true,reasons:[]});});it('uses role weights for joint mission consensus',async()=>{const flow={...StrategicTwinOrchestrationPattern,missionId:'mission-1',assignments:StrategicTwinOrchestrationPattern.assignments.map(a=>({...a,missionId:'mission-1'}))};const result=await new MissionOrchestrator({record:()=>{}}).authorizeJointIntent(flow,[{proposalId:'mission-1',twinId:'twin-strategist-01',decision:'ACCEPT',timestamp:'x'},{proposalId:'mission-1',twinId:'twin-simulator-01',decision:'ACCEPT',timestamp:'x'}]);expect(result).toEqual({reached:true,result:'ACCEPT'});});it('rejects invalid personality weights',()=>{const invalid={...StrategicTwinPersonality,traits:[{name:'reckless',weight:2}]};expect(validateTwinConfiguration(StrategicTwinIdentity,invalid,StrategicTwinRoleContract,StrategicTwinSovereignty).ok).toBe(false);});});
