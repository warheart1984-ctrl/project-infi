import type { KernelStatus, SubstratePlan } from '../common/models.js'; import type{EventPublisher}from'../events/EventBus.js'; import type { PlanChecker } from './checkers.js';
export class PlanKernel { constructor(private readonly checkers: readonly PlanChecker[],private readonly events?:EventPublisher) {} validatePlan(p:SubstratePlan):KernelStatus { const reasons=this.checkers.flatMap(c=>c.check(p)); const status={ok:reasons.length===0,reasons};if(status.ok)void this.events?.publish({id:`evt-plan-${p.planId}`,type:'PLAN_APPROVED',actorId:p.actorId,intentId:p.intentId,planId:p.planId,timestamp:new Date().toISOString(),payload:p as never});return status; } }
