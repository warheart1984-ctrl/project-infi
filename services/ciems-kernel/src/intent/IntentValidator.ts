import type { Intent, KernelStatus } from '../common/models.js';
export interface IntentDependencies { actorExists(id: string): boolean; intentTypeExists(type: string): boolean; contextExists(id: string): boolean; actorAuthorized(actorId: string, contextId: string): boolean }
export class IntentValidator { constructor(private readonly deps: IntentDependencies) {} validate(i: Intent): KernelStatus { const r: string[]=[]; if(!i.intentId)r.push('Missing intentId'); if(!i.actorId)r.push('Missing actorId'); if(!i.type)r.push('Missing type'); if(!i.contextSnapshotId)r.push('Missing contextSnapshotId'); if(i.actorId&&!this.deps.actorExists(i.actorId))r.push('Unknown actorId'); if(i.type&&!this.deps.intentTypeExists(i.type))r.push('Intent type is not registered in ISL'); if(i.contextSnapshotId&&!this.deps.contextExists(i.contextSnapshotId))r.push('Unknown contextSnapshotId'); if(i.actorId&&i.contextSnapshotId&&!this.deps.actorAuthorized(i.actorId,i.contextSnapshotId))r.push('Actor is outside sovereignty scope'); return {ok:r.length===0,reasons:r}; } }
