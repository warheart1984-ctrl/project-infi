import type { Intent, KernelStatus } from '../common/models.js'; import type { AuditLogger } from '../common/logger.js'; import type{EventPublisher}from'../events/EventBus.js'; import { IntentValidator } from './IntentValidator.js';
export class IntentKernel { constructor(private readonly validator: IntentValidator, private readonly audit: AuditLogger,private readonly events?:EventPublisher) {} validateIntent(i: Intent): KernelStatus { const s=this.validator.validate(i); const at=new Date().toISOString(); void this.audit.record({gate:'intent',subjectId:i.intentId, ...s, at}); if(s.ok)void this.events?.publish({id:`evt-intent-${i.intentId}`,type:'INTENT_ACCEPTED',actorId:i.actorId,intentId:i.intentId,timestamp:at,payload:i as never}); return s; } }
