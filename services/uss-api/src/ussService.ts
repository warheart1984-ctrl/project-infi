import type{GraphClient,GraphId}from'./graphClient.js';export class USSService{constructor(private readonly graph:GraphClient){}query(r:{graph:GraphId;filter?:Parameters<GraphClient['query']>[1]}){return this.graph.query(r.graph,r.filter);}replay(r:{graph:GraphId;at:string;scope:{actorId?:string;missionId?:string}}){return this.graph.snapshotAt(r.graph,r.at,r.scope);}trajectory(r:{graph:GraphId;rootScenarioId:string;depth:number}){return this.graph.trajectory(r.graph,r.rootScenarioId,r.depth);}}
