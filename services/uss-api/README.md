# USS API

Typed query, point-in-time replay, and bounded multiverse trajectory service over an injected property-graph driver. Graph labels are allowlisted; actor and mission filters remain parameterized.
