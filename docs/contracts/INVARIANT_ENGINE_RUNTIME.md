# Invariant Engine Runtime Contract

Status: **active contract**

## Runtime Surfaces

- `validate_bridge_packet` — fail-closed on cognitive bridge ingress
- `validate_realtime_event_prediction` — advisory on predictor output
- Nova comparison via `invariant_engine_organ` on companion turns

## Scope

Math layer is complete; runtime wiring extends bridge + chat finalize paths.
