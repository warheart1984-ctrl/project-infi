{
  "schema_version": "scorpion.event.v1",
  "description": "Kernel Sentinel MUST emit only this shape; no direct ledger writes",
  "required_fields": ["ts_ns", "domain", "actor", "payload"],
  "optional_fields": ["lineage_id"],
  "valid_domains": [
    "syscall_sequence",
    "scheduler_rhythm",
    "memory_lifecycle",
    "fd_flow",
    "ipc_choreography",
    "privilege_transition",
    "entropy_signature",
    "timing_delta"
  ]
}
