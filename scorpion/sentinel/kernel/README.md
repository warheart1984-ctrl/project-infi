# Kernel Sentinel (Stage 4)

Native observer code is not vendored in this repository.

See `docs/subsystems/scorpion/KERNEL_SENTINEL_DESIGN.md` for adapter boundaries and promotion criteria.

Use `scorpion.sentinel.kernel_stub.KernelSentinelStub` until VM proof bundles exist.
