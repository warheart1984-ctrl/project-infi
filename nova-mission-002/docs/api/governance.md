# Governance API

- `trace()` — full governance trace
- `requireInvariant(inv)` — register invariant
- `getReceipt(id)` — fetch receipt
- `validateAction(action)` — pre-flight validation
- `listReceipts()` — all receipts
