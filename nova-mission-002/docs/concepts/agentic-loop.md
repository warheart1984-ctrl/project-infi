# Agentic Loop

```
observe workspace → gather context → propose plan → validate invariants
  → for each step: validate → execute → record receipt → update continuity
```

See `examples/basic-project/src/agent-loop.ts` for a reference implementation.
