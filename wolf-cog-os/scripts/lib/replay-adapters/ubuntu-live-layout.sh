#!/usr/bin/env bash
# Ubuntu live uses debian-live-layout paths (P10).
# shellcheck source=replay-adapters/debian-live-layout.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/debian-live-layout.sh"
